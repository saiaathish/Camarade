# Hero fixture plan

Create a small Next.js/TypeScript fixture with App Router routes, existing auth/billing modules, middleware, public and protected handlers, and tests.

Seed conflicting global/per-handler instructions, stale `pages/api` wording, and a dependency recommendation contradicted by existing infrastructure. Task fixture must require HTTP 429. Capture source hashes, expected findings, protected-file paths, validation commands, and expected changed-file boundary before any agent run.
