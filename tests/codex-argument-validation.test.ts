import { describe, expect, it } from "vitest";import { validateCodexArguments } from "../src/experiment/validate-codex-arguments.js";describe("Codex arguments",()=>{it("requires one model",()=>{expect(validateCodexArguments(["--model","fake"]).model).toBe("fake");expect(()=>validateCodexArguments([])).toThrow();expect(()=>validateCodexArguments(["--model","a","-m","b"])).toThrow();});});
