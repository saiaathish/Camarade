import { describe, expect, it } from "vitest";import { mkdtemp } from "node:fs/promises";import { join } from "node:path";import { tmpdir } from "node:os";import { createConditionRuntime } from "../src/experiment/create-condition-runtime.js";describe("condition runtime",()=>{it("keeps artifacts under condition directory",async()=>{const r=await createConditionRuntime(await mkdtemp(join(tmpdir(),"s5-03-")),"baseline");expect(r.promptPath.startsWith(r.conditionDirectory)).toBe(true);});});
