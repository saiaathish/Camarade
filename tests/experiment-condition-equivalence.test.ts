import { describe, expect, it } from "vitest";
import { validateConditionEquivalence } from "../src/experiment/validate-condition-equivalence.js";
describe("condition equivalence",()=>{it("fails identical context treatments",()=>{const work=(path:string)=>({conditionId:"baseline" as const,path,startingCommit:"a",startingTree:"b",trackedTreeHash:"c",clean:true as const});const context=(conditionId:"baseline"|"camarade",path:string)=>({conditionId,contextKind:conditionId==="baseline"?"original-repository" as const:"camarade-compiled" as const,contextPath:path,contextHash:"same",sourcePaths:[]});const base={conditionId:"baseline" as const,worktree:work("/tmp/base"),context:context("baseline","/tmp/ctx1"),taskHash:"d",codexConfigurationHash:"e",validationConfigurationHash:"f",contextBudgetHash:"g"};const cam={...base,conditionId:"camarade" as const,worktree:{...work("/tmp/cam"),conditionId:"camarade" as const},context:context("camarade","/tmp/ctx2")};expect(()=>validateConditionEquivalence(base,cam,{controllerRoot:"/tmp",experimentDirectory:"/tmp",worktreeDirectory:"/tmp",baselineWorktreePath:"/tmp/base",camaradeWorktreePath:"/tmp/cam",baselineContextDirectory:"/tmp/ctx1",camaradeContextDirectory:"/tmp/ctx2",preparationResultPath:"/tmp/r",startingStatePath:"/tmp/s",fairnessAuditPath:"/tmp/a"})).toThrow();});});
