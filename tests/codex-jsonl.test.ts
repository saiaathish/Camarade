import { describe, expect, it } from "vitest";import { parseCodexJsonl } from "../src/experiment/parse-codex-jsonl.js";describe("Codex JSONL",()=>{it("preserves malformed count and explicit usage",()=>{const x=parseCodexJsonl('{"type":"done","usage":{"input_tokens":1,"output_tokens":2}}\nbad\n');expect(x.validJsonLines).toBe(1);expect(x.invalidJsonLines).toBe(1);expect(x.actualTokenUsageAvailable).toBe(true);});});
