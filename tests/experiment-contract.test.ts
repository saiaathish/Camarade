import { describe, expect, it } from "vitest";
import { buildFairExperimentSpecification } from "../src/experiment/build-experiment-spec.js";
import type { LoadedRunConfigWithContext } from "../src/config/load-run-config.js";
const config=(order:"baseline-first"|"camarade-first"):LoadedRunConfigWithContext=>({configPath:null,validationCommands:["npm test"],timeoutSeconds:1,experiment:{instructionMode:"augmentation",executionOrder:order,codex:{executable:"codex",timeoutSeconds:2,arguments:[],environmentAllowlist:[]}}});
describe("fair experiment specification",()=>{it("is canonical and deterministic",()=>{const a=buildFairExperimentSpecification({repositoryPath:"/tmp/repo",task:"  Add rate limiting to public search API\n",experimentId:"one"},config("baseline-first")); const b=buildFairExperimentSpecification({repositoryPath:"/tmp/repo",task:"  Add rate limiting to public search API\n",experimentId:"two"},config("baseline-first")); expect(a.conditions.map(x=>x.conditionId)).toEqual(["baseline","camarade"]); expect(a.orderedConditionIds).toEqual(["baseline","camarade"]); expect(a.task.original).toBe("  Add rate limiting to public search API\n"); expect(a.specificationHash).toBe(b.specificationHash); expect(a.specificationId).toBe(b.specificationId); expect(a.specificationHash).toMatch(/^[0-9a-f]{64}$/); expect(JSON.stringify(a)).not.toMatch(/winner|score|timestamp|worktree|gitSha|tokenSavings/);});});
