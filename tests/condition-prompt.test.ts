import { describe, expect, it } from "vitest";import { renderConditionPrompt } from "../src/experiment/render-condition-prompt.js";describe("condition prompt",()=>{it("preserves task and context bytes",()=>{const x=renderConditionPrompt({conditionId:"baseline",task:" a\n",taskHash:"a",context:" b\n",contextHash:"b",promptPath:"/tmp/p"});expect(x.prompt).toContain(" a\n");expect(x.prompt).toContain(" b\n");expect(x.prompt.endsWith("\n")).toBe(true);});});
