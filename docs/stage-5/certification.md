# Stage 5 certification

Run `npm run certify:stage5` for build and deterministic MCP discovery. Live certification is separately gated by `CAMARADE_AUTHORIZE_LIVE_CODEX=YES` and runs through `npm run certify:stage5:live -- --repo <absolute-path> --task <task>`.
