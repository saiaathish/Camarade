# 2026-07-16 build log

## Stage 6 deterministic experiment measurement

Completed the Stage 6 measurement pipeline on top of the existing Stage 6 definition sealing and Stage 5 integrity verification work.

Implemented:

- Disposable Git worktrees reconstructed from the shared starting commit and each recorded Stage 5 patch.
- Identical sealed hidden-asset overlays applied only after candidate change evidence is captured.
- Bounded command execution with saved stdout, stderr, structured results, timeouts, process-tree termination, environment hashes, and explicitly declared structured-test parsing.
- Deterministic correctness, requirement, rule, changed-file, dependency, token, and runtime measurements.
- Fixed 40/25/20/10/5 scoring, inclusive one-point ties, and material correctness/rule/requirement overrides.
- Valid, limited, and invalid resolution with no winner for incomplete or untrustworthy evidence.
- Non-overwriting category JSON, comparison JSON, integrity report, Markdown report, and hashed evidence index.
- `camarade.measure_experiment`, a shared CLI wrapper, server version 1.2.0, protocol verification, and full Stage 4 → Stage 5 → Stage 6 certification.
- A hero evaluation definition matched to the real fixture plus a sealed deterministic reset-window test.

Integrity decisions:

- Stage 6 never evaluates inside completed Stage 5 worktrees and never reruns Codex.
- Candidate change and dependency metrics are captured before hidden assets or evaluation commands can create files.
- Adapter token telemetry is used only when present; missing values remain `unavailable` and are never estimated.
- Console output is not scraped for test counts.
- Unsealed legacy experiments remain measurable but limited and cannot return a benchmark winner.
- Hard evaluator failures preserve a failure artifact and failed stage.

## Validation

- `npm install` — PASS; npm reported the existing 5 audit findings (3 moderate, 1 high, 1 critical) and pending install-script approval notices. No dependency or audit rewrite was attempted.
- `npm run typecheck` — PASS.
- `npm test -- --reporter=json --outputFile=/tmp/camarade-stage6-vitest.json` — PASS: 89 files, 870 tests, 0 failures, 0 skipped.
- `npm run build` — PASS.
- `npm run verify:stage4` — PASS.
- `npm run verify:stage5` — PASS.
- `npm run verify:stage6` — PASS; all three MCP tools were discovered and malformed or unconfirmed Stage 6 calls were rejected.
- `npm run certify:stage5` — PASS.
- `npm run certify:stage6` — PASS through a real MCP client: Stage 4 compilation, matched fake-Codex Stage 5 execution, Stage 6 measurement, artifact readback, and cleanup all completed.
- `git diff --check` — PASS.

The Stage 6 certification outcome was a deterministic tie in a temporary fake-Codex protocol fixture. It proves the measurement plumbing and artifact agreement only; it is not a real Camarade benchmark or agent-quality claim.
