# Build log — 2026-07-14

## Stage 2 implementation record

Stage 2 implements the local vertical slice from repository scan through preserved comparison evidence. This entry records implementation decisions and validation evidence for orchestrator review. It makes no benchmark claim.

## Major decisions

- Kept scanner, compiler, experiment controller, evaluator, artifacts, and CLI independent of any named coding agent. Execution is behind the `AgentAdapter` interface.
- Exposed only the implemented `fixture` and `command` adapters. The fixture adapter is a deterministic simulation; the command adapter starts an explicitly configured process without a shell.
- Required clean Git input, one shared starting commit, separate detached condition worktrees, an external controller root, and non-overwriting comparison paths.
- Preserved the baseline's tracked and Git-ignored active instructions. Archived original context outside both worktrees, removed active original instructions from the Camarade worktree, and installed only the compiled root `AGENTS.md` there.
- Stored generated context, per-condition logs, raw diffs, metrics, complete manifests, and one summary under the controller-owned run directory. Removed temporary worktrees while preserving run evidence.
- Required every manifest field to contain measured evidence or a nonempty `unavailableReason`. Current unknowns include model, adapter version, exact permissions, token budget, and environment hash.
- Collected raw changed-file, line, dependency-file, validation, exit-code, and duration evidence. Kept summaries `invalid-or-limited` because numeric comparison tolerance is undeclared.
- Preserved failure evidence when a layout exists, recorded typed pipeline stages, continued after validation failures, represented timeouts with null exit codes, and refused artifact overwrite or unsafe cleanup.
- Kept command-adapter arguments literal with `shell: false`. Kept configured validation commands sequential and shell-executed; both paths receive an allowlisted base environment and still require trusted configuration.
- Applied review hardening: allowlisted child-process environment variables; killed timed-out process trees; recorded nonfatal termination errors; bounded process timers and Git evidence output; captured staged and untracked Git patches; excluded nested control instructions from metrics; rejected non-HEAD comparisons, symlink escapes, unsafe worktree destinations, and symlinked run configs; preserved Git-ignored active instructions in baseline only; hid worktree Git metadata during adapter execution; held original context in memory until execution ended; removed baseline before Camarade; verified active instructions after adapter and validation execution; checked original HEAD; wrote JSON artifacts atomically; and emitted honest failure manifests and validation placeholders when execution stopped early.

## Hero fixture decision

The fixture generator creates a clean committed repository from `examples/hero-fixture-template` with deterministic Git metadata. The template contains conflicting dependency guidance, a stale route path, protected auth and billing files, existing middleware and rate-limit code, and an HTTP 429 test.

The fixture adapter intentionally applies predefined changes: the baseline adds a dependency, changes protected auth behavior, and fails the fixture validation; the Camarade condition reuses middleware, leaves protected/dependency files unchanged, and passes. These results are simulated and are not benchmark evidence.

## AI-assisted delegated work

AI-assisted work used GPT-5.6 Sol High agents with disjoint path ownership: S2-03 hero fixture, S2-04 scanner, S2-05 compiler, S2-06 experiment/artifact controller, S2-07 adapters, S2-08 evaluator, S2-09 pipeline/CLI integration, S2-10 vertical-slice and failure tests, and S2-11 documentation. One documentation attempt was stopped after making no changes; a replacement completed the owned documentation files.

Independent review and blocker repair used fresh GPT-5.6 Luna xhigh agents for contract integrity, code safety, test adequacy, context-path safety, controller/artifact safety, and bounded Git evidence. Reviewer findings were verified against current code; confirmed blockers received regression coverage before re-review.

The orchestrator reviewed and integrated every patch, corrected the adapter method to the required `execute` contract, rejected controller roots inside target repositories, added runtime manifest-evidence completeness checks, reconciled the legacy CLI smoke test with the real command interface, and reran the integrated suite. Final contract-integrity, code-safety, and test-adequacy reviews each returned `VALID: YES`; the final focused contract review confirmed that comparison notices are caller-supplied rather than fixture-specific. No score, winner, or generalized agent-performance conclusion was inferred from the fixture.

## Validation

- `npm test` — PASS on 2026-07-14: 18 test files passed; 98 tests passed.
- `npm run typecheck` — PASS on 2026-07-14.
- `git diff --check -- README.md docs/stage-2/vertical-slice.md docs/build-log/2026-07-14.md` — PASS on 2026-07-14.
- Script names were checked against `package.json`; CLI flags and evaluation syntax were checked against `src/cli.ts`; artifact paths and manifest behavior were checked against the Stage 2 implementation and tests.

Operational acceptance produced two independent controller evidence trees from the same committed fixture:

- `/private/tmp/camarade-final-one.jRzhPD/.camarade/runs/comparison-49fc360fe444c0519b07`
- `/private/tmp/camarade-final-two.ZuTjBl/.camarade/runs/comparison-49fc360fe444c0519b07`

Both operational runs passed their expected condition validations, retained complete manifests and labeled simulation evidence, removed their worktrees, and matched structurally after normalization of permitted timestamps, IDs, durations, and temporary absolute paths.

The acceptance count above is a repository test result only. It is not a benchmark result.

## Product website

Added an independent `frontend/` Vite/React product site for the public Camarade narrative. It is intentionally isolated from the root controller package and preserves the repository's evidence boundary: the site uses the illustrative rate-limit fixture from the product spec, labels the build as early, and does not claim an installable runtime, benchmark result, or production integration that the repository does not provide.

The frontend was verified with `npm run typecheck`, `npm run build`, `npm run qa:visual`, and the offline frontend audit. The visual QA pass covered 1440px, 768px, 640px as a 200%-zoom reflow equivalent, 390px, and 320px, including keyboard focus retention, rapid Replay activation, reduced motion, axe-core, link integrity, no SVG elements, no horizontal overflow, and zero console/HTTP/request errors.
## S3-06 fixture and proof

Added a separate `examples/intelligence-fixture` and `tests/s3-06-intelligence-e2e.test.ts`. The targeted proof reads real fixture files with a 100,000-byte context limit and exercises the implemented intelligence pipeline through recommendations. It does not alter the Stage 2 hero fixture, production files, dependencies, network state, Stage 4, or persistent artifacts. Results remain fixture evidence only; no benchmark or runtime claim is made.

## Stage 4 task-specific context compiler

Stage 4 now compiles one natural-language coding task and Stage 3 repository intelligence into a bounded, evidence-backed JSON contract plus a Markdown rendering. The new `compile` path remains separate from the legacy Stage 2 comparison controller and the independent `frontend/`. It does not execute Codex or another agent, run validation commands, score implementations, claim a benchmark result, expose a dashboard, or fabricate token usage.

Two verified Stage 3 prerequisites were repaired without changing the `1.0.0` wire shape. `IntelligenceArtifact.graph` now uses the node/edge graph that is actually serialized, its real content contributes to artifact identity, and structural node/edge/reference validation replaced the former type bridge. The evaluator now separates structural validity from actionable open-conflict status, so open conflicts remain valid Stage 4 input. Integrated CLI tests invoke `src/cli.ts` through the installed `tsx` loader, and the vertical-slice reproducibility comparison normalizes volatile TAP timing text rather than depending on a prior build.

The Stage 4 implementation adds the canonical contracts under `src/context/`, structured task normalization, broad evidence-backed retrieval, deterministic exclusions, a provider-neutral reasoner boundary with a deterministic fixture implementation, final conflict/deduplication resolution, Unicode-character and item budgets, contract validation, pure JSON-to-Markdown rendering, external atomic artifact publication, and the `compile` CLI command. Optional `context_compiler.budget` parsing preserves the legacy Stage 2 configuration return shape when absent. Missing intelligence is generated in memory; an explicitly requested artifact is strict and must match task, repository inventory, content-derived identity, graph entity kinds, and cross-kind provenance.

Review hardening kept scope exclusions separate from applicability scopes, prevented exempted paths from becoming protected context, made explicit task instructions outrank directly contradictory repository modification restrictions, kept equal directly relevant protected conflicts unresolved, removed unrelated conflicts from blocking output, and collapsed exact or Stage 3-proven semantic duplicates to one evidenced representative. The reasoner accepts only candidate-owned evidence, request-owned relationships, an allowlisted machine vocabulary, and evidence-neutral explanation templates. A dedicated read-only repository fingerprint covers generated directories, large/binary files, permissions, empty directories, and symbolic-link identity; only top-level `.git` and `.camarade` control state is excluded.

The hero fixture adds a reusable rate-limit response utility, billing noise, a meaningful HTTP 429 utility assertion, stale/deleted guidance, and equally supported fixed-window versus sliding-window rules. The operational contract selects shared middleware, `src/public-search.ts`, `src/rate-limit.ts`, `tests/public-search.test.ts`, the protected auth restriction, and `npm test`. It excludes stale per-handler guidance, the deleted route, dependency guidance, billing noise, irrelevant authentication implementation context, and a redundant protected-rule copy. The fixed/sliding policy remains one unresolved group backed by two unresolved candidate decisions.

### Stage 4 validation evidence

- `npm install` — PASS, already up to date. npm reported 5 audit findings (3 moderate, 1 high, 1 critical) and pending install-script approval notices for two `esbuild` versions and `fsevents`; no out-of-scope dependency or audit rewrite was attempted.
- `npm run typecheck` — PASS.
- `npm test` — PASS: 52 test files and 537 tests.
- `npm run build` — PASS.
- `git diff --check` — PASS.
- Public operational compile run one — PASS: 44 candidates, 18 included, 24 excluded, 2 unresolved candidate decisions, and 9,969 of 12,000 Unicode characters. Evidence is retained under `/private/var/folders/l5/d0z3k_ys6k1438bnft2gqy880000gn/T/camarade-controller-gUR2os`.
- Public operational compile run two — PASS with the same counts and budget. Evidence is retained under `/private/var/folders/l5/d0z3k_ys6k1438bnft2gqy880000gn/T/camarade-controller-4BMWPo`.
- Cross-run comparison — PASS: task specification, candidates, deterministic decisions, exclusions, unresolved decisions, selected candidate/evidence sets, and Markdown were identical; normalized contract structure matched; each bundle contained the same nine files; compilation IDs differed as allowed. The shared Markdown SHA-256 is `719b2fa132adcd8cc51a6f4647c8dd6f7e680d41cea2b4e5373674f7caddae00`.

Three read-only review tracks covered the Stage 1/Stage 4 boundary, evidence/provenance and hallucination resistance, and test/determinism adequacy. Confirmed blockers received focused regressions and affected-suite reruns before the complete suite. This record reports local validation only: the repository has no configured GitHub Actions workflow, so it does not claim CI is green.
