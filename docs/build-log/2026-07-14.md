# Build log — 2026-07-14

## Stage 2 implementation record

Stage 2 implements the local vertical slice from repository scan through preserved comparison evidence. This entry records implementation decisions and validation evidence for orchestrator review. It makes no benchmark claim.

## Major decisions

- Kept scanner, compiler, experiment controller, evaluator, artifacts, and CLI independent of any named coding agent. Execution is behind the `AgentAdapter` interface.
- Exposed only the implemented `fixture` and `command` adapters. The fixture adapter is a deterministic simulation; the command adapter starts an explicitly configured process without a shell.
- Required clean Git input, one shared starting commit, separate detached condition worktrees, an external controller root, and non-overwriting comparison paths.
- Preserved the baseline's tracked and Git-ignored active instructions. Archived original context outside both worktrees, removed active original instructions from the Camarade worktree, and installed only the compiled root `AGENTS.md` there.
- Stored generated context, per-condition logs, raw diffs, metrics, complete manifests, and one summary under the controller-owned run directory. Removed temporary worktrees while preserving run evidence.
- Required every manifest field to contain measured evidence or a nonempty `unavailableReason`. Current unknowns include model, adapter version, exact permissions, token budget, and environment hash.
- Collected raw changed-file, line, dependency-file, validation, exit-code, and duration evidence. Kept summaries `invalid-or-limited` because numeric comparison tolerance is undeclared.
- Preserved failure evidence when a layout exists, recorded typed pipeline stages, continued after validation failures, represented timeouts with null exit codes, and refused artifact overwrite or unsafe cleanup.
- Kept command-adapter arguments literal with `shell: false`. Kept configured validation commands sequential and shell-executed; both paths receive an allowlisted base environment and still require trusted configuration.
- Applied review hardening: allowlisted child-process environment variables; killed timed-out process trees; recorded nonfatal termination errors; bounded process timers and Git evidence output; captured staged and untracked Git patches; excluded nested control instructions from metrics; rejected non-HEAD comparisons, symlink escapes, unsafe worktree destinations, and symlinked run configs; preserved Git-ignored active instructions in baseline only; hid worktree Git metadata during adapter execution; held original context in memory until execution ended; removed baseline before Camarade; verified active instructions after adapter and validation execution; checked original HEAD; wrote JSON artifacts atomically; and emitted honest failure manifests and validation placeholders when execution stopped early.

## Hero fixture decision

The fixture generator creates a clean committed repository from `examples/hero-fixture-template` with deterministic Git metadata. The template contains conflicting dependency guidance, a stale route path, protected auth and billing files, existing middleware and rate-limit code, and an HTTP 429 test.

The fixture adapter intentionally applies predefined changes: the baseline adds a dependency, changes protected auth behavior, and fails the fixture validation; the Camarade condition reuses middleware, leaves protected/dependency files unchanged, and passes. These results are simulated and are not benchmark evidence.

## AI-assisted delegated work

AI-assisted work used GPT-5.6 Sol High agents with disjoint path ownership: S2-03 hero fixture, S2-04 scanner, S2-05 compiler, S2-06 experiment/artifact controller, S2-07 adapters, S2-08 evaluator, S2-09 pipeline/CLI integration, S2-10 vertical-slice and failure tests, and S2-11 documentation. One documentation attempt was stopped after making no changes; a replacement completed the owned documentation files.

Independent review and blocker repair used fresh GPT-5.6 Luna xhigh agents for contract integrity, code safety, test adequacy, context-path safety, controller/artifact safety, and bounded Git evidence. Reviewer findings were verified against current code; confirmed blockers received regression coverage before re-review.

The orchestrator reviewed and integrated every patch, corrected the adapter method to the required `execute` contract, rejected controller roots inside target repositories, added runtime manifest-evidence completeness checks, reconciled the legacy CLI smoke test with the real command interface, and reran the integrated suite. Final contract-integrity, code-safety, and test-adequacy reviews each returned `VALID: YES`; the final focused contract review confirmed that comparison notices are caller-supplied rather than fixture-specific. No score, winner, or generalized agent-performance conclusion was inferred from the fixture.

## Validation

- `npm test` — PASS on 2026-07-14: 18 test files passed; 98 tests passed.
- `npm run typecheck` — PASS on 2026-07-14.
- `git diff --check -- README.md docs/stage-2/vertical-slice.md docs/build-log/2026-07-14.md` — PASS on 2026-07-14.
- Script names were checked against `package.json`; CLI flags and evaluation syntax were checked against `src/cli.ts`; artifact paths and manifest behavior were checked against the Stage 2 implementation and tests.

Operational acceptance produced two independent controller evidence trees from the same committed fixture:

- `/private/tmp/camarade-final-one.jRzhPD/.camarade/runs/comparison-49fc360fe444c0519b07`
- `/private/tmp/camarade-final-two.ZuTjBl/.camarade/runs/comparison-49fc360fe444c0519b07`

Both operational runs passed their expected condition validations, retained complete manifests and labeled simulation evidence, removed their worktrees, and matched structurally after normalization of permitted timestamps, IDs, durations, and temporary absolute paths.

The acceptance count above is a repository test result only. It is not a benchmark result.
