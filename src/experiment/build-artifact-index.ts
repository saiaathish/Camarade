import { lstat, readdir, readFile } from "node:fs/promises"; import { relative, resolve } from "node:path"; import { canonicalJson, sha256, toPosixPath } from "../context/context-serialization.js"; import type { ExperimentArtifactIndex, ExperimentArtifactIndexEntry } from "./experiment-types.js";
async function walk(root:string, dir:string, out:string[]):Promise<void>{for(const entry of await readdir(dir,{withFileTypes:true})){const p=resolve(dir,entry.name); if(entry.name.startsWith(".")&&entry.name.endsWith(".tmp"))continue; if(entry.isSymbolicLink())throw new Error(`Artifact symlink rejected: ${p}`); if(entry.isDirectory())await walk(root,p,out); else if(entry.isFile())out.push(p); else throw new Error(`Unsupported artifact entry: ${p}`);}}
export async function buildArtifactIndex(experimentDirectory:string, experimentId:string):Promise<ExperimentArtifactIndex>{const root=resolve(experimentDirectory), paths:string[]=[]; await walk(root,root,paths); const excluded=new Set(["artifact-index.json","experiment-manifest.json","experiment-summary.json","experiment-result.json"]); const entries:ExperimentArtifactIndexEntry[]=[]; for(const p of paths){const rel=toPosixPath(relative(root,p)); if(excluded.has(rel))continue; const bytes=await readFile(p); const stat=await lstat(p); entries.push({relativePath:rel,kind:"other",sha256:sha256(bytes),byteLength:stat.size});} entries.sort((a,b)=>a.relativePath.localeCompare(b.relativePath)); return {schemaVersion:"1.0.0",experimentId,entries,entriesHash:sha256(canonicalJson(entries))};}
