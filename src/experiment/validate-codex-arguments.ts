import { ExperimentExecutionError } from "./experiment-errors.js";
export interface ValidatedCodexArguments { arguments:string[]; model:string; }
export function validateCodexArguments(arguments_:readonly string[]):ValidatedCodexArguments { const args=[...arguments_];if(args.some(a=>typeof a!=="string"||a===""||a.includes("\0")))throw new ExperimentExecutionError("Invalid Codex arguments.","EXPERIMENT_CODEX_CONFIG_INVALID","argument-validation");let model:string|undefined;for(let i=0;i<args.length;i++){const a=args[i]!;if(["exec","e","--cd","-C","--json","--experimental-json","--output-last-message","-o","--output-schema","--sandbox","-s","--ask-for-approval","-a","--ephemeral","--ignore-user-config","--ignore-rules","--full-auto","--yolo","--dangerously-bypass-approvals-and-sandbox","--dangerously-bypass-hook-trust","--skip-git-repo-check","--search","--add-dir","-"].includes(a)||/^--config(?:=|$)/.test(a)||a==="-c")throw new ExperimentExecutionError("Configured Codex arguments contain a reserved control flag.","EXPERIMENT_CODEX_CONFIG_INVALID","argument-validation");if(a==="--model"||a==="-m"){if(model!==undefined||!args[i+1])throw new ExperimentExecutionError("Exactly one explicit model is required.","EXPERIMENT_CODEX_CONFIG_INVALID","argument-validation");model=args[++i]!;}else if(a.startsWith("--model=")){if(model!==undefined||a.slice(8)==="")throw new ExperimentExecutionError("Exactly one explicit model is required.","EXPERIMENT_CODEX_CONFIG_INVALID","argument-validation");model=a.slice(8);}}if(!model)throw new ExperimentExecutionError("Exactly one explicit model is required.","EXPERIMENT_CODEX_CONFIG_INVALID","argument-validation");return {arguments:args,model};}
