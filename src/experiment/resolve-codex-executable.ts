import { access, realpath, stat } from "node:fs/promises";import { constants } from "node:fs";import { delimiter, isAbsolute, join, resolve } from "node:path";import { ExperimentExecutionError } from "./experiment-errors.js";
export async function resolveCodexExecutable(executable:string,environment:NodeJS.ProcessEnv):Promise<string>{if(!executable||executable.includes("\0"))throw new ExperimentExecutionError("Codex executable is invalid.","EXPERIMENT_CODEX_UNAVAILABLE","executable-resolution");const candidates=isAbsolute(executable)?[executable]:(environment.PATH??"").split(delimiter).filter(Boolean).map(p=>join(p,executable));for(const candidate of candidates){try{const path=await realpath(resolve(candidate));const info=await stat(path);if(!info.isFile())continue;if(process.platform!=="win32")await access(path,constants.X_OK);return path;}catch{}}throw new ExperimentExecutionError("Codex executable is unavailable.","EXPERIMENT_CODEX_UNAVAILABLE","executable-resolution");}
export async function resolveCodexVersion(executable:string,args:readonly string[],environment:NodeJS.ProcessEnv):Promise<string>{const {spawn}=await import("node:child_process");return await new Promise((resolveVersion,reject)=>{const child=spawn(executable,[...args,"--version"],{env:environment,shell:false,stdio:["ignore","pipe","pipe"]});let output="";child.stdout.on("data",chunk=>{output+=String(chunk);});child.stderr.on("data",chunk=>{output+=String(chunk);});const timer=setTimeout(()=>{child.kill("SIGTERM");reject(new ExperimentExecutionError("Codex version resolution timed out.","EXPERIMENT_CODEX_VERSION_FAILED","version-resolution"));},10000);child.on("error",error=>{clearTimeout(timer);reject(new ExperimentExecutionError("Codex version resolution failed.","EXPERIMENT_CODEX_VERSION_FAILED","version-resolution",{},undefined,error));});child.on("close",code=>{clearTimeout(timer);const line=output.split(/\r?\n/).map(v=>v.trim()).find(Boolean);if(code!==0||!line)reject(new ExperimentExecutionError("Codex version output was unavailable.","EXPERIMENT_CODEX_VERSION_FAILED","version-resolution"));else resolveVersion(line);});});}
