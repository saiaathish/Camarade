import type { ExperimentInstructionSource, FairExperimentSpecification, ExperimentStartingState, ExperimentPreparationLayout, PreparedConditionManifest } from "./experiment-types.js";
export function buildConditionManifests(spec: FairExperimentSpecification, state: ExperimentStartingState, layout: ExperimentPreparationLayout, baseline: { contextHash: string; sources: ExperimentInstructionSource[]; sourcePaths: string[] }, camarade: { contextHash: string; stage4CompilationId: string; stage4ContractHash: string; stage4ArtifactPaths: object }): { baseline: PreparedConditionManifest; camarade: PreparedConditionManifest } {
  const common = { taskHash: spec.task.sha256, codexConfigurationHash: spec.hashes.codexConfiguration, validationConfigurationHash: spec.hashes.validationConfiguration, contextBudgetHash: spec.hashes.contextBudget };
  const work = (conditionId: "baseline" | "camarade", path: string) => ({ conditionId, path, startingCommit: state.startingCommit, startingTree: state.startingTree, trackedTreeHash: "", clean: true as const });
  return { baseline: { ...common, conditionId: "baseline", worktree: work("baseline", layout.baselineWorktreePath), context: { conditionId: "baseline", contextKind: "original-repository", instructionMode: spec.instructionMode, contextPath: layout.baselineContextDirectory, contextHash: baseline.contextHash, sourcePaths: baseline.sourcePaths, sources: baseline.sources, includesOriginalInstructions: true, requiresNativeInstructionSuppression: false } }, camarade: { ...common, conditionId: "camarade", worktree: work("camarade", layout.camaradeWorktreePath), context: { conditionId: "camarade", contextKind: "camarade-compiled", instructionMode: spec.instructionMode, contextPath: layout.camaradeContextDirectory, contextHash: camarade.contextHash, sourcePaths: [], includesOriginalInstructions: spec.instructionMode === "augmentation", requiresNativeInstructionSuppression: spec.instructionMode === "replacement", stage4CompilationId: camarade.stage4CompilationId, stage4ContractHash: camarade.stage4ContractHash, stage4ArtifactPaths: Object.values(camarade.stage4ArtifactPaths) } } };
}
