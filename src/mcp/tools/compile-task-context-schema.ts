import { z } from "zod/v3";
import { hasPortableParentTraversal, isPortableAbsolutePath } from "../../core/path-portability.js";
const safeText = (label: string) => z.string().refine((value) => value.trim().length > 0, `${label} must not be blank`).refine((value) => !value.includes("\0"), `${label} must not contain null bytes`);
export const compileTaskContextSchema = z.object({ repository_root: safeText("repository_root").refine(isPortableAbsolutePath, "repository_root must be absolute"), task: safeText("task"), context_budget: z.number().int().safe().positive().optional(), intelligence_artifact: safeText("intelligence_artifact").refine((value) => !isPortableAbsolutePath(value), "intelligence_artifact must be repository-relative").refine((value) => value !== "<task>", "intelligence_artifact must not be <task>").refine((value) => !hasPortableParentTraversal(value), "intelligence_artifact must not traverse").optional() }).strict();
export type CompileTaskContextSchemaInput = z.infer<typeof compileTaskContextSchema>;
