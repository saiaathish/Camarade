export const CAMARADE_MCP_INSTRUCTIONS = "Camarade has three tools. Use compile_task_context for context-only compilation. Use run_fair_experiment only after explicit user approval; it executes matched Codex conditions and never declares a winner. Use measure_experiment only for a completed Stage 5 comparison with the exact evaluation-command authorization statement. measure_experiment verifies sealed evidence, evaluates disposable sandboxes with deterministic checks, writes reproducible JSON and Markdown evidence, and returns no benchmark winner when the experiment is invalid or limited.";
