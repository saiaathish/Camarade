export const CAMARADE_MCP_INSTRUCTIONS = "Camarade has two tools. Use compile_task_context for context-only compilation. Use run_fair_experiment only after explicit user approval. run_fair_experiment executes Codex twice, runs repository validation commands, requires a clean Git repository, reads execution settings from camarade.run.yaml, creates external artifacts and temporary worktrees, removes experiment worktrees after evidence capture, and never declares a winner or score. Use returned evidence for audit only.";
