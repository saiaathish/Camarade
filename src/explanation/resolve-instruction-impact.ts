import type { EvidenceReference, EvidenceStrength, ImpactDirection } from "./instruction-evidence-types.js";
import type { Obj } from "./instruction-impact-match.js";
export interface ImpactResolution { direction: ImpactDirection; strength: EvidenceStrength; limitations: string[]; }
export const resolveInstructionImpact = (positive: Obj[], negative: Obj[], contradictory: Obj[], status?: "valid"|"limited"|"invalid"): ImpactResolution => {
  const limitations: string[] = []; if (status === "limited") limitations.push("Limited experiment evidence supports only a bounded conclusion."); if (contradictory.length) return { direction: "unknown", strength: "insufficient", limitations: [...limitations, "Higher-priority contradictory evidence blocks the impact claim."] }; if (status === "invalid") return { direction: "unknown", strength: "insufficient", limitations: [...limitations, "Invalid experiment evidence cannot establish helped or hurt."] }; if (negative.length) return { direction: "hurt", strength: positive.length ? "strongly-supported" : "direct", limitations }; if (positive.length) return { direction: "helped", strength: positive.length > 1 ? "strongly-supported" : "direct", limitations }; return { direction: "neutral", strength: status === "limited" ? "correlated" : "insufficient", limitations: [...limitations, "Instruction presence, file change, dependency presence, score, token, or runtime evidence alone is non-causal."] };
};
